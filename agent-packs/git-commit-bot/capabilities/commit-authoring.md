Create a focused git commit from the intended changes.

Use a concise subject line and a body only when it adds useful context. Avoid vague messages such as "update" or "fix stuff". Do not include secrets, local machine paths, or unrelated metadata in commit messages.
