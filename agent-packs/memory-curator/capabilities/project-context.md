Use the repository and workspace context to avoid generic memory.

When summarizing, include concrete file paths, commands, local conventions, and implementation decisions only when they are likely to guide future work. Avoid restating obvious facts that can be rediscovered cheaply from the code.
