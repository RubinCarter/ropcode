# 功能视角重构清单

日期：2026-05-22

## 功能清单

| 功能域                        | 当前主要旧代码                                                                                                                                                                                                                                                                      | 建议新边界                                                                                                             | 旧代码删除条件                                                                                |
| ----------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------- |
| RPC 门面 / API 分组           | `bindings.go` 6058 行；`frontend/src/lib/rpc-client.ts` 1626 行                                                                                                                                                                                                                 | 后端拆成 `bindings_*.go`；前端拆成 `frontend/src/lib/rpc/*.ts`                                                     | 所有 RPC wrapper 和后端方法都从新文件编译通过；`go test ./...` 和前端 typecheck 通过        |
| Provider 会话运行时           | `internal/claude/session.go` 1356 行；`internal/codex/session.go` 960 行；`internal/gemini/session.go` 677 行；`internal/deepseek/session.go` 580 行                                                                                                                        | 抽共享运行时到 `internal/sessionproc` 或 `internal/provider/runtime`，负责进程启动、输出读取、状态、环境变量处理   | 各 provider 的参数构造、输出转换、取消、终止都有测试覆盖；旧重复 helper 没有引用              |
| 会话 UI / 消息流              | `frontend/src/components/ai-code-session/AiCodeSession.tsx` 1867 行；`frontend/src/components/ai-code-session/hooks/useSessionEvents.ts` 568 行；`frontend/src/components/ai-code-session/hooks/useMessages.ts` 543 行；`frontend/src/components/StreamMessage.tsx` 1470 行 | 保持在 `ai-code-session/` 下，拆成 state、events、message rendering、virtual list 等模块                             | UI 自动化确认会话打开、流式输出、恢复、切换 provider 正常                                     |
| 工具调用展示                  | `frontend/src/components/ToolWidgets.tsx` 3373 行；`frontend/src/components/SessionOutputViewer.tsx` 755 行；`frontend/src/components/AgentRunOutputViewer.tsx` 831 行                                                                                                        | 新建 `frontend/src/components/tool-widgets/`，每类 tool 一个小组件，共用折叠、路径、代码块组件                       | 各 tool 类型渲染有组件测试或快照覆盖；旧 registry 文件没有调用方                              |
| Prompt 输入 / 模型选择 / 附件 | `frontend/src/components/FloatingPromptInput.tsx` 2297 行；`frontend/src/components/SlashCommandPicker.tsx` 598 行；`frontend/src/components/FilePicker.tsx` 625 行；`frontend/src/components/ClaudeCapabilityPicker.tsx` 617 行                                            | 新建 `frontend/src/components/prompt-input/`：composer、provider selector、model selector、attachments、slash picker | 现有 prompt input 测试通过，并补足关键交互覆盖；旧组件缩成 re-export 或直接删除               |
| 项目 / 工作区 / Git           | `frontend/src/components/ProjectList.tsx` 1399 行；`frontend/src/components/containers/WorkspaceContainer.tsx` 669 行；`cmd/ropcode/context.go` 710 行；`cmd/ropcode/session.go` 760 行                                                                                     | 建立 `project-workspace` 功能模块，覆盖 project tree、workspace actions、Git state、CLI project resolution           | CLI PWD 行为和 UI project list sync 测试通过，不依赖手动刷新                                  |
| 数据库 / 存储管理             | `internal/database/db.go` 1288 行；`frontend/src/components/StorageTab.tsx` 912 行                                                                                                                                                                                              | 后端按表或聚合拆分，但保留 `Database` 类型；前端拆 storage admin 子模块                                              | `db.go` 只保留 open/init/schema 和共享 helper；storage admin 仍支持表读取、写入、查询、重置 |
| 设置中心                      | `frontend/src/components/Settings.tsx` 1245 行；`frontend/src/components/ProviderApiManager.tsx` 463 行；`frontend/src/components/ModelManager.tsx` 563 行；`frontend/src/components/HooksEditor.tsx` 930 行                                                                | 新建 `frontend/src/components/settings/`：tabs shell、provider API、models、hooks、debug、storage                    | 每个 tab 独立懒加载，保存流程测试通过；旧 `Settings.tsx` 只剩设置页壳                       |
| Agents / 自动执行             | `frontend/src/components/CreateAgent.tsx` 546 行；`frontend/src/components/CCAgents.tsx` 579 行；`frontend/src/components/AgentExecution.tsx` 1095 行；`bindings.go` 6058 行中包含 agent RPC 方法                                                                           | 新建 `frontend/src/components/agents/`，后端 agent RPC 单独分组，覆盖 CRUD、run lifecycle、output、import/export     | agent 创建、执行、取消、导入、导出流程通过；旧页面级混合逻辑没有引用                          |
| Claude 配置资产               | `frontend/src/components/ClaudeAgentsManager.tsx` 631 行；`internal/claude/capability_discovery.go` 649 行；`internal/claude/commands.go` 577 行；`bindings.go` 6058 行中包含 capability/slash/hooks/CLAUDE.md 方法                                                         | 建立 `claude-config/` 模块，覆盖 capabilities、slash commands、agents、hooks、CLAUDE.md 编辑                         | Claude 专属配置不再散落在通用 Settings 或巨型 RPC 文件里                                      |
| MCP / 插件 / Skills           | `internal/mcp/manager.go` 581 行；`internal/plugin/plugin.go` 585 行；`bindings.go` 6058 行中包含 MCP/plugin/skills 方法                                                                                                                                                      | 分别建立 `mcp`、`plugins`、`skills` API 分组                                                                     | 插件页、MCP 添加/测试、skills list/get 流程通过                                               |
| 文件浏览 / 打开方式 / WebView | `frontend/src/components/WebViewer.tsx` 896 行；`frontend/src/components/WebViewWidget.tsx` 898 行；`frontend/src/components/right-sidebar/index.tsx` 1037 行；`bindings.go` 6058 行中包含 file/open 方法                                                                   | 建立 `workspace-tools/`：file explorer、web viewer、open-in actions、terminal actions                                | 文件读写、外部打开、webview 导航测试通过                                                      |
| 使用量 / 分析                 | `frontend/src/components/UsageDashboard.tsx` 726 行；`frontend/src/hooks/useAnalytics.ts` 661 行；`frontend/src/lib/analytics/events.ts` 700 行；`internal/usage/stats.go` 559 行                                                                                           | 建立 `analytics/`：事件定义、采集 hook、usage RPC、dashboard                                                         | 事件定义从 UI 中分离；usage 查询测试通过                                                      |
| SSH 同步                      | `frontend/src/components/SyncFromSSHDialog.tsx` 620 行；`bindings.go` 6058 行中包含 SSH 方法                                                                                                                                                                                    | 建立 `ssh-sync/`：connections、sync actions、status polling                                                          | sync from/to、auto sync、pause、resume、cancel 的 RPC 和 UI 流程通过                          |
| 会话标题 / 分支命名           | `session_title.go` 1153 行                                                                                                                                                                                                                                                        | 新建 `internal/sessiontitle/`：prompt 构造、provider adapter、title store、branch naming                             | `App` 方法只做委托；标题清洗、API provider、CLI provider 有聚焦测试                         |
| 样式系统                      | `frontend/src/styles.css` 1070 行                                                                                                                                                                                                                                                 | 拆成 `styles/base.css`、`styles/layout.css`、`styles/components.css`，或按功能 colocate                          | 第一轮只移动不改样式；UI 自动化和关键截图确认没有视觉回归                                     |
